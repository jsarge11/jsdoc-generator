# Props JSON Generator

## To Run
1. `npm i`
2. `npm start`
3. Check the generated props folder!

### Interfaces must begin with `export interface props` and each prop description should come before the name, and formatted in JSDoc syntax.

